# 🚀 Инструкция по размещению проекта на Streamlit Cloud через GitHub

## Шаг 1: Подготовка файлов проекта

### 1.1 Создайте файл `.gitignore`

Создайте файл `.gitignore` в корне проекта:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
streamlit_env/
*.egg-info/
dist/
build/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb

# IDE
.vscode/
.idea/
*.swp
*.swo

# Данные (большие файлы)
data/
*.csv
*.pkl
*.pickle

# Временные файлы
.DS_Store
*.log
*.tmp

# Streamlit
.streamlit/secrets.toml
```

### 1.2 Создайте `requirements.txt` для деплоя

Создайте файл `requirements.txt` в корне проекта (или используйте существующий `requirements_streamlit.txt`):

```txt
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.17.0
joblib>=1.3.0
```

### 1.3 Создайте конфигурационный файл Streamlit (опционально)

Создайте файл `.streamlit/config.toml`:

```toml
[theme]
primaryColor = "#667eea"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"

[server]
headless = true
port = 8501
```

### 1.4 Подготовка файла модели

**ВАЖНО:** Файл `best_pipeline_final.pkl` слишком большой (108 MB) для GitHub. У вас есть несколько вариантов:

#### Вариант A: Использовать Git LFS (рекомендуется для больших файлов)
```bash
# Установите Git LFS
git lfs install

# Добавьте отслеживание для .pkl файлов
git lfs track "*.pkl"

# Добавьте .gitattributes в .gitignore (НЕ добавляйте!)
git add .gitattributes
```

#### Вариант B: Загрузить модель отдельно (через Streamlit Secrets или внешнее хранилище)
Создайте инструкцию для пользователей загружать модель отдельно.

#### Вариант C: Использовать демо-режим
Приложение будет работать в демо-режиме, если модель не найдена.

## Шаг 2: Создание репозитория на GitHub

### 2.1 Создайте новый репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку **"New repository"** или **"+" → "New repository"**
3. Заполните информацию:
   - **Repository name**: `lithology-prediction-app` (или любое другое имя)
   - **Description**: "Система предсказания литологии на основе геологических параметров"
   - Выберите **Public** (для бесплатного Streamlit Cloud) или **Private**
   - **НЕ** создавайте README, .gitignore или лицензию (мы это сделаем локально)
4. Нажмите **"Create repository"**

### 2.2 Инициализация Git в локальном проекте

Откройте терминал в директории проекта:

```bash
cd "/media/user/Programs/C/Курс 4/Диплом/Research"

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Streamlit app for lithology prediction"

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/lithology-prediction-app.git

# Установите основную ветку
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Если используете Git LFS для модели:**
```bash
# После git init и git lfs install
git lfs track "*.pkl"
git add .gitattributes
git add .
git commit -m "Initial commit with Git LFS for model file"
git push -u origin main
```

## Шаг 3: Подключение к Streamlit Cloud

### 3.1 Войдите в Streamlit Cloud

1. Перейдите на https://share.streamlit.io
2. Войдите через GitHub аккаунт (нажмите **"Sign in with GitHub"**)
3. Авторизуйте Streamlit Cloud доступ к вашим репозиториям

### 3.2 Создайте новое приложение

1. Нажмите **"New app"**
2. Заполните форму:
   - **Repository**: выберите ваш репозиторий `lithology-prediction-app`
   - **Branch**: выберите `main` (или `master`)
   - **Main file path**: `streamlit_app.py`
   - **App URL**: будет автоматически создан (например: `lithology-prediction-app.streamlit.app`)
3. Нажмите **"Deploy!"**

### 3.3 Настройка (опционально)

После создания приложения можно настроить:
- **Settings** → **App configuration**: изменить URL, добавить описание
- **Settings** → **Secrets**: добавить секретные ключи (если нужны)

## Шаг 4: Проверка деплоя

1. Дождитесь завершения деплоя (обычно 1-2 минуты)
2. Streamlit Cloud автоматически:
   - Установит зависимости из `requirements.txt`
   - Запустит приложение
   - Покажет логи в интерфейсе
3. Если есть ошибки, проверьте логи в разделе **"Manage app" → "Logs"**

## Шаг 5: Решение проблем

### Проблема: Файл модели слишком большой

**Решение 1: Используйте Git LFS**
```bash
git lfs install
git lfs track "*.pkl"
git add .gitattributes best_pipeline_final.pkl
git commit -m "Add model file with LFS"
git push
```

**Решение 2: Загрузите модель через secrets**
1. Загрузите модель в облачное хранилище (Google Drive, Dropbox, S3)
2. Добавьте ссылку в Streamlit Secrets
3. Обновите код для загрузки модели из URL

**Решение 3: Удалите модель из репозитория**
- Приложение будет работать в демо-режиме
- Пользователи могут загрузить модель локально

### Проблема: Ошибки при деплое

**Проверьте:**
1. Файл `requirements.txt` существует и содержит все зависимости
2. Главный файл `streamlit_app.py` находится в корне репозитория
3. Все импорты корректны
4. Нет синтаксических ошибок в Python коде

**Просмотр логов:**
- В Streamlit Cloud: **"Manage app" → "Logs"**
- Проверьте ошибки импорта, отсутствующие файлы и т.д.

### Проблема: Модель не загружается

**Решение:**
1. Убедитесь, что `best_pipeline_final.pkl` загружен в репозиторий
2. Проверьте путь к файлу в коде (относительный путь от корня)
3. Если используете Git LFS, убедитесь, что файл загружен через LFS

## Шаг 6: Обновление приложения

При каждом новом коммите в GitHub, Streamlit Cloud автоматически перезапустит приложение:

```bash
# Внесите изменения в код
# ...

# Закоммитьте изменения
git add .
git commit -m "Update app: add new features"
git push

# Streamlit Cloud автоматически обновит приложение через 1-2 минуты
```

## Полезные ссылки

- **Streamlit Cloud**: https://share.streamlit.io
- **Документация Streamlit Cloud**: https://docs.streamlit.io/streamlit-community-cloud
- **Git LFS**: https://git-lfs.github.com/
- **GitHub**: https://github.com

## Структура проекта для GitHub

```
Research/
├── streamlit_app.py          # Главный файл приложения
├── requirements.txt          # Зависимости (или requirements_streamlit.txt)
├── .streamlit/
│   └── config.toml          # Конфигурация Streamlit (опционально)
├── .gitignore               # Игнорируемые файлы
├── best_pipeline_final.pkl  # Модель (через Git LFS)
├── best_hyperparameters.json # Гиперпараметры
├── README.md                # Описание проекта
└── DEPLOY_STREAMLIT.md      # Эта инструкция
```

## Готово! 🎉

После выполнения всех шагов ваше приложение будет доступно по адресу:
`https://YOUR_APP_NAME.streamlit.app`

